public class Matriks {
    //ATRIBUT
    int rows;
    int cols;
    double[][] matrix;

    //KONSTRUKTOR
    public Matriks(int rows, int cols) {
        this.rows = rows;
        this.cols = cols;
        this.matrix = new double[rows][cols];
        }

    //METHOD PRIMITIF MATRIKS
    public readMatriks(int rows, int cols) {
        int i, j;
        Scanner sc = new Scanner(System.in);
        for (i=0;i<N;i++) {
            for (j=0; j<M; j++) {

                this.matrix[i][j] = 
            }
        }
    }

    //METHOD GAUSS DAN KAWAN KAWAN
}